[package]
name = "seam-lowering"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Lowers a reduced Svelte markup tree into the Seam IR"

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
