# fs-router-demo

Filesystem router demo with all route types: dynamic params, optional params, catch-all, route groups, and nested layouts.

## Structure

- `src/pages/` — page files using convention-based naming
- `seam.toml` — config with `build.pages_dir = "src/pages"`

## Development

- Run: `seam dev`
