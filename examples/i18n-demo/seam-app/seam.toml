[project]
name = "i18n-demo"

[backend]
lang = "typescript"

[frontend]
entry = "src/client/main.tsx"

[build]
backend_build_command = "true"
bundler_command = "bunx vite build"
bundler_manifest = ".seam/dist/.vite/manifest.json"
router_file = "src/server/router.ts"
routes = "./src/client/routes.ts"
out_dir = ".seam/output"

[i18n]
locales = ["en", "zh"]
default = "en"
cache = true
