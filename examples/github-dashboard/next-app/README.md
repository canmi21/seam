# github-dashboard-next-app

Next.js SSR comparison for the [GitHub Dashboard](../README.md), demonstrating the same UI without SeamJS.

## Structure

- `app/` — Next.js App Router pages and layout
- `next.config.ts` — Transpile shared package, `.js` → `.tsx` alias

## Development

- Dev: `bun run dev` (port 3001)
