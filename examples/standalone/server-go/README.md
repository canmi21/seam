# demo-server-go

Minimal Go backend using `net/http`, demonstrating procedure, subscription, and page registration with the SeamJS Go server core.

## Structure

- `main.go` — Server entry and router setup
- `procedures/` — Example procedure handlers
- `subscriptions/` — SSE subscription handler
- `pages/` — Page definitions with loaders

## Development

- Run: `go run .`
