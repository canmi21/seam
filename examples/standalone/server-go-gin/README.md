# demo-server-go-gin

Go/Gin variant of [demo-server-go](../server-go/), reusing the same procedure definitions with Gin routing.

## Structure

- `main.go` — Gin router setup with procedure registration

## Development

- Run: `go run .`
