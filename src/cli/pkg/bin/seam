#!/usr/bin/env node
/* src/cli/pkg/bin/seam */

const { spawnSync } = require("child_process");
const { findBinary } = require("../lib/find-binary.cjs");

const binary = findBinary() || "seam";
const result = spawnSync(binary, process.argv.slice(2), {
  stdio: "inherit",
  shell: false,
});

if (result.error) {
  if (result.error.code === "ENOENT") {
    console.error(
      `seam: binary not found for ${process.platform}-${process.arch}.\n` +
        "Install via cargo: cargo install seam-cli",
    );
  } else {
    console.error("Failed to execute seam CLI:", result.error);
  }
  process.exit(1);
}

process.exitCode = result.status;
